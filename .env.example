# Live P2000 RSS polling (true = live, false = mock/seed data)
REAL_DATA=false

# Server port
PORT=3001

# SQLite database directory (default: server/data)
# DATA_DIR=/app/data

# Restrict CORS to your domain in production (default: * = allow all)
# ALLOWED_ORIGIN=https://ramptoerist.133t.nl
